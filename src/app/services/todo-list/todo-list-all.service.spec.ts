import { TestBed } from '@angular/core/testing';

import { TodoListAllService } from './todo-list-all.service';

describe('TodoListAllService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: TodoListAllService = TestBed.get(TodoListAllService);
  //   expect(service).toBeTruthy();
  // });
});
